name: Lint yml files

on:
    push:
    pull_request:

jobs:
    yamllint:
        runs-on: ubuntu-20.04
        steps:
            - name: Frenck's yamllint
              # You may pin to the exact commit or the version.
              # uses: frenck/action-yamllint@57b4ef7a8959708336293b59cce0367952382980
              uses: frenck/action-yamllint@v1.4.0
