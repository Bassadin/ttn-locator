---
version: "3"
services:
    db:
        extends:
            file: ./docker-compose.base.yml
            service: db
    pgadmin:
        extends:
            file: ./docker-compose.base.yml
            service: pgadmin

volumes:
    postgres-data:
    pgadmin-data:
