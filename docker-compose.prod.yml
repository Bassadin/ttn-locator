---
version: "3"
services:
    ttnmapper-reader:
        extends:
            file: docker-compose.base.yml
            service: ttnmapper-reader
    db:
        extends:
            file: docker-compose.base.yml
            service: db
    pgadmin:
        extends:
            file: docker-compose.base.yml
            service: pgadmin

volumes:
    postgres-data:
    pgadmin-data:
